import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  card: {
    maxWidth: 400,
  },
  removeBtn: {
    display: 'block',
    top: '50%',
    left: '50%',
    transform: 'translate(-50%, -50%)',
  },
});
